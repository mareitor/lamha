# Copy to .env.local for local dev, and set the same key as a Netlify
# environment variable for the deployed site (Site settings > Environment
# variables). Must point at the deployed Worker's URL, no trailing slash.
VITE_API_BASE_URL=https://lamha-worker.<your-subdomain>.workers.dev
